{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: pytest",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/tests/test_run_login.py", // Path to your test file
      "args": ["-m", "pytest"],
      "justMyCode": true
    }
  ]
}
